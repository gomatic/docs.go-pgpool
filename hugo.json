{
  "baseURL": "https://gomatic.github.io/docs.go-pgpool/",
  "title": "go-pgpool",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-pgpool — tuned pgx v5 connection pools and health checking for PostgreSQL and CockroachDB."
  }
}
